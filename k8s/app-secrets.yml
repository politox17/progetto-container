# Nota: valori in base64 (esempio: echo -n "user" | base64).
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
data:
  db_user: dXNlcg==        # "user (esempio, da modificare)"
  db_password: cGFzc3dvcmQ= # "password (esempio, da modificare)"