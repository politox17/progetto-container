# Nota: valori in base64 (esempio: echo -n "user" | base64).
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  POSTGRES_USER: "libreria"
  POSTGRES_PASSWORD: "libreria"
  RABBITMQ_DEFAULT_USER: "libreria"
  RABBITMQ_DEFAULT_PASS: "libreria"